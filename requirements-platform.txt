# EcoGrid OS — Phase 2 Platform Core (consumer + API)
#
# Layered on the Phase 1 base so the shared contract dependencies (pydantic,
# aiokafka) resolve identically in both processes. The ingestion worker's own
# image installs only `requirements.txt` and stays slim.
#
#   pip install -r requirements-platform.txt

-r requirements.txt

# --- HTTP service ---
fastapi==0.115.0
uvicorn[standard]==0.31.1

# --- PostgreSQL (async) ---
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0

# --- Redis (async) ---
redis==5.1.1

# --- Test tooling for the API surface ---
httpx==0.27.2

# --- Schema migrations ---
# `ecogrid.migrate` (create_all) bootstraps a brand-new database; Alembic is how
# every subsequent change is applied. See alembic/versions/0001_baseline.py.
alembic==1.13.3

# --- Optional: real MLflow tracking for the AI Orchestrator ---
# Uncomment to enable ECOGRID_MLFLOW_TRACKING_URI. Without it the orchestrator
# logs runs to a local JSONL file, so this stays optional to keep the image slim.
# mlflow==2.16.0
